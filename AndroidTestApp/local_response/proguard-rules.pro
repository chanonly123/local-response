# Rules for minifying this library itself. Minification is off for the release
# variant, so this file is here to be the declared target of `proguardFiles`
# rather than to do any work; the rules consumers need are in
# `consumer-rules.pro`, which is packaged into the AAR.
